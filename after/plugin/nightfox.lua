require('nightfox').setup({
    options = {
        styles = {
            comments     = "italic",
            conditionals = "italic",
            keywords     = "italic",
        }
    }
})

vim.cmd("colorscheme carbonfox")
